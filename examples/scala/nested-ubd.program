// Validating Farrow's use-before-declaration (nested scopes)
x = 10;
y = x + 20;
x = z / 30;
z = 4 + b; // b is not in scope
{
  a = b + c;
  b = 42;
  d = e + 1;
  e = d - 1;
  c = x * 2;
  f = g + h;
  h = a / 2;
}
