module TEST[] begin
  type Test := LIST[String];
  
  t : Test := Test$single("hello,") ++ Test$single(" world");

  i : Integer := length("hello");
  
  b : Boolean := t = Test${} and Test${} = t;
end;


-- module FOO[] begin
--     var bar : Integer := 3;
-- end;

-- signature GOO := FOO[];

-- module QUOD[T :: GOO] extends T begin
--    var quux : Integer := bar;
-- end;
